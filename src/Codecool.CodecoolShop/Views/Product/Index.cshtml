@*@{
    ViewData["Title"] = "Home Page";
}
*/


@model List<Codecool.CodecoolShop.Models.Product>

<div class="container">
    @foreach (var element in Model)
    {
        <div class="col-lg-3 col-lg-3 cardData" style="display: inline-block; max-width: 350px; height: 350px" 
             data-supplier="@element.Supplier.Name" data-category="@element.ProductCategory.Department" data-price="@element.DefaultPrice."
             data-name="@element.Name" data-description="@element.Description">
            <div class="card">
                <img src="img/@(element.Name).jpg" style="height: 50%; width: 50%; align-self: center; padding-top: 10px">

/**@